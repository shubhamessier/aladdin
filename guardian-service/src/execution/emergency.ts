import type { ProposedAction } from '../blockchain/writer.js';
export const emergencyHandler = {
    generateActions: (state: string, portfolio: unknown, riskMetrics: unknown) => [] as ProposedAction[],
};
