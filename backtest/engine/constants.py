# Market capitalization proxies used for equilibrium return calculations (CAPM prior)
# Values represent approximate relative market share in the asset universe.
MARKET_CAP_PRIORS = {
    "BTC": 0.60,
    "ETH": 0.25,
    "SOL": 0.05,
    "USDC": 0.05,
    "USDT": 0.05,
    "DAI": 0.02,
}

# Default risk aversion parameter (lambda)
DEFAULT_RISK_AVERSION = 2.5
